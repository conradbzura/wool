"""shapebench — a generalization-across-shapes benchmark suite for task-execution frameworks.

See METHODOLOGY.md for the design. The public surface is the shape-level
``Adapter`` interface (adapter.py), the shape ladder registry (shapes.py), and
the CLI runner (cli.py).
"""

__version__ = "0.0.1"
